// Project metadata type definitions
export interface Metadata {
  name: string;
  repository: string;
  description: string;
  author: string;
  license: string;
  version: string;
  build: number;
}
